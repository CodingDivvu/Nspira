#include<stdio.h>
int main()
{
    int i,n,j,k,arr[n],max,min;
    printf("Enter the limit of the array:");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(i=0;i<n;i++)
    {
        printf("\n%d",arr[i]);
    }
    for(i=0;i<n;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if(arr[i]<arr[j])
            {
                temp=a[i]
            }
        }
    }
}